<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fcbox.mapper.UserXmlMapper">
    <select id="queryEdmUserId" resultType="java.lang.String">
        select edm_user_id from edm_user where user_mobilephone=#{mobilePhone}
    </select>
</mapper>